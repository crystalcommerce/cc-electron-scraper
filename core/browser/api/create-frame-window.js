const { registerWindowEvent } = require("../../../utilities");
const FrameWindow = require("../classes/frame-window");
const childProcess = require('child_process');

module.exports = function(parentWindowObject, resourceLocation, windowOptions = {}) {

    // let frameWindow = new FrameWindow(parentWindowObject, resourceLocation, windowOptions);

    
}